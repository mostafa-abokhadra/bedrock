get:
  tags:
    - User Authentication
  summary: get csrf token need for any state changing request (e:g post put delete)
  responses:
    200:
      description: successfully got csrf token
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/auth/csrfTokenSchemas.yaml#/successfullCSRF'