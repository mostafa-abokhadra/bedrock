post:
  tags:
    - User Authentication
  summary: Register a new user
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/auth/signupRequest.yaml#/signupRequest'
          # $ref: './components.yaml#/components/schemas/signupRequest'
  responses:
    201:
      description: User created successfully